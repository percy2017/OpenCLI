# OpenCLI

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (Express API + WebSocket server)
#API server
SERVER_PORT=3001
#Frontend port
VITE_PORT=5173

# Host/IP to bind servers to (default: 0.0.0.0 for all interfaces)
# Use 127.0.0.1 to restrict to localhost only
HOST=0.0.0.0

# Public address browsers use to reach the server. Must match what the user
# types in the URL bar (e.g. `http://192.168.0.10:3001`). When set, Vite's dev
# proxy routes /api, /ws, /shell, and /plugin-ws to the backend via this
# address so WebSocket upgrades don't get lost between hosts.
# PROXY_HOST=http://192.168.0.10:3001

# Uncomment the following line if you have a custom claude cli path other than the default "claude"
# CLAUDE_CLI_PATH=claude

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Path to the authentication database file
# This is where user credentials, API keys, and tokens are stored.
#
# To use a custom location:
DATABASE_PATH=./database/auth.db
#
# Claude Code context window size (maximum tokens per session)
VITE_CONTEXT_WINDOW=1000000
CONTEXT_WINDOW=1000000

# =============================================================================
# FILE ATTACHMENTS (chat composer)
# =============================================================================
# Max number of files (images + office) attached per chat message
VITE_MAX_FILE_ATTACHMENTS=5
# Max size per file in MB
VITE_MAX_FILE_ATTACHMENT_SIZE_MB=10
# Server-side limits (multer) — must match or exceed the VITE_* values
MAX_FILE_ATTACHMENTS=5
MAX_FILE_ATTACHMENT_SIZE_MB=10

# =============================================================================
# WORKSPACE ROOT
# =============================================================================
# Directory the project-creation wizard and /api/browse-filesystem treat as the
# safe browsing root. Defaults to os.homedir() (i.e. /root when running as
# root), which then collides with the FORBIDDEN_WORKSPACE_PATHS list and the
# folder picker returns 403. Pin it to a non-system path here.
WORKSPACES_ROOT=/

# =============================================================================
# RAG — EMBEDDING PROVIDER
# =============================================================================
# Which provider to use for vectorizing documents and queries.
# Supported: ollama (local, default) | minimax | openai
RAG_EMBEDDING_PROVIDER=ollama

# Ollama (default). Runs on localhost:11434 by default; install with
# `curl -fsSL https://ollama.com/install.sh | sh` then `ollama pull mxbai-embed-large`.
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_EMBEDDING_MODEL=mxbai-embed-large
# How long Ollama keeps the model loaded after a request. 5m avoids re-loading
# the model on every chat ping.
OLLAMA_KEEP_ALIVE=5m
# Ollama batch size per request (sent as a single `input` array).
OLLAMA_EMBEDDING_BATCH_SIZE=32
# Retry on transient errors before failing the index job.
OLLAMA_EMBEDDING_RETRIES=2

# Generic chunking params (apply to any provider).
RAG_CHUNK_SIZE=512
RAG_CHUNK_OVERLAP=50

# =============================================================================
# UI TABS
# =============================================================================
# Show the "Terminal" (agent shell, xterm.js) tab in the main content header.
# Set to "false" to hide it. The other "Terminal" (the Terminal module) is
# controlled independently from Settings → Terminal.
# Default: true
VITE_SHOW_SHELL_TAB=false

# =============================================================================
# TTS — READ-ALOUD BUTTON IN CHAT
# =============================================================================
# Drives the "Reproducir" button that appears next to the copy button on each
# assistant message. The button calls POST /api/tts/synthesize, which shells
# out to the `mmx` CLI (already authenticated via MMX_API_KEY / ~/.mmx/config.json).
#
# Set TTS_ENABLED to "false" to disable the feature entirely (the button still
# renders but every click returns 503). Default: true.
TTS_ENABLED=true

# Default voice for read-aloud. See available voices (332 total; 47 in Spanish):
#   mmx speech voices --output json
#   mmx speech voices --language spanish --output json
TTS_VOICE=Spanish_Narrator

# Speech speed multiplier (0.5 - 2.0). 1.0 is natural pace.
TTS_SPEED=1.0

# Language boost passed to mmx (en, es, zh, ja, ko, pt, ...).
TTS_LANGUAGE=es

# Model ID. speech-2.8-hd is highest quality; speech-02 is cheapest.
TTS_MODEL=speech-2.8-hd

# Auto-play assistant responses as soon as they finish streaming.
# Set to "true" to enable by default. Users can stop playback per-message via
# the play button. Default: false.
TTS_AUTO_PLAY=false

# Synthesis timeout (mmx call) in ms. Default: 30000.
TTS_TIMEOUT_MS=30000

# =============================================================================
# WHISPER — VOICE TRANSCRIPTION (chat voice message button)
# =============================================================================

# Set to "false" to hide the mic button entirely. Default: true.
# WHISPER_ENABLED=true

# Absolute path to whisper-cli binary. Auto-detected on first install.
# WHISPER_BINARY=/opt/opencli/server/whisper/build/build/bin/whisper-cli

# Model path or filename. Default: ggml-base.bin in the whisper models dir.
# WHISPER_MODEL=ggml-base.bin

# Language hint passed to whisper.cpp (-l). Default: auto.
# WHISPER_LANGUAGE=auto

# Hard timeout for each transcription call in ms. Default: 60000.
# WHISPER_TIMEOUT_MS=60000
